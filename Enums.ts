// In TypeScript, enums (short for "enumerations") are a special data type that defines a set of named constants. These constants, often called "members" or "values," represent a fixed set of possible values for a particular property or variable.



enum Direction {
    Up,
    Down,
    Left,
    Right
}

let myDirection: Direction = Direction.Up;

